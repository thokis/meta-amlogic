#@TYPE: Machine
#@NAME: aml-s805x-ac machine
#@DESCRIPTION: Reference machine configuration for the amlogic p241 reference machine

PREFERRED_PROVIDER:virtual/kernel ?= "linux-yocto"
KERNEL_DEVICETREE ?= "amlogic/meson-gxl-s805x-p241.dtb"
UBOOT_MACHINE ?= "p241_defconfig"
MACHINE_FEATURES:append = " alsa ext2 screen usbgadget usbhost vfat"

# Only possibility on this device
NOSEM_BOOT_DEV = "emmc"

require conf/machine/include/amlogic-s805x.inc

# Use wic on this machine
require conf/machine/include/nosem-wic.inc
