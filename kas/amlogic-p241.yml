header:
  version: 10
  includes:
    - poky.yml
    - nosem.yml

machine: amlogic-p241
distro: poky
target:
  - core-image-base

local_conf_header:
  testing: |
    EXTRA_IMAGE_FEATURES_append = " \
      ssh-server-dropbear \
      allow-empty-password \
      empty-root-password \
      splash"
    IMAGE_INSTALL_append = " \
      alsa-utils \
      mpg123"
